sudo: required

language: c

services:
  - docker

before_install:
  - docker build -f Dockerfile -t atgreen/moxielogic-repos .
  - docker run --rm -t -i -v `pwd`:/opt atgreen/moxielogic-repos /root/build.sh

script:
- 
